<!DOCTYPE html>
<html>
<head>
  <title></title>
</head>
<body>


  <div class="container mt-5">
    <h1 class="h3 mb-3 font-weight-normal text-center"> Les participants : </h1>
      <% @attend.each do |participant| %>
          <div class="card mt-3 mb-3">
            <div class="card-body">
              <p class="card-text">
                <%= participant.user.first_name %> <%= participant.user.last_name %>
              </p>
            </div>
          </div>
      <% end %>

    <div class="mt-5">
      <%= link_to "Editer mon évènement", edit_event_path(params[:event_id]), class: 'btn btn-outline-dark' %>
    </div>
  </div>

</body>
</html>